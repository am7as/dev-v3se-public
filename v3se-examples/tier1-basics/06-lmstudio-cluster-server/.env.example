# 06-lmstudio-cluster-server — .env.example
# Inherits the canonical contract from _shared/env/.env.template.

# Container paths — do not change
DATA_DIR=/data
RESULTS_DIR=/results
MODELS_DIR=/models

# Host paths (laptop). Leave blank for sibling defaults.
DATA_HOST=
RESULTS_HOST=
MODELS_HOST=

# C3SE cluster
CEPHYR_USER=
CEPHYR_PROJECT_PATH=
MIMER_GROUP_PATH=
MIMER_PROJECT_PATH=
ALVIS_ACCOUNT=
CEPHYR_TRANSFER_HOST=vera2.c3se.chalmers.se
ALVIS_LOGIN_HOST=alvis2.c3se.chalmers.se

# LM Studio
# Model identifier as it appears in the LM Studio catalog / local cache.
LMSTUDIO_MODEL=lmstudio-community/llama-3.1-8b-instruct

# Where LM Studio stores downloaded weights. On Alvis, point at Mimer.
# Example: /mimer/NOBACKUP/groups/naiss2025-22-321/<cid>/lmstudio
LMSTUDIO_CACHE_DIR=

# OpenAI-compatible client — defaults used by client.py when not set
OPENAI_API_KEY=lm-studio
OPENAI_BASE_URL=
