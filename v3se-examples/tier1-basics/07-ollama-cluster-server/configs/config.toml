[project]
name        = "infer-api"
description = "V3SE template: LLM inference via API token"

[paths]
data_dir    = "/data"
results_dir = "/results"
models_dir  = "/models"

[provider.openai]
default_model = "gpt-4o-mini"
