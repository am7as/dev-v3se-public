[workspace]
name = "infer-multi"
version = "0.1.0"
description = "V3SE template: multi-provider LLM inference"
authors = ["v3se-template contributors <noreply@example.com>"]
channels = ["conda-forge"]
platforms = ["linux-64", "win-64", "osx-arm64"]

[dependencies]
python = "3.12.*"
pip = "*"

[pypi-dependencies]
infer_multi = { path = ".", editable = true }
openai      = ">=1.50"
pyyaml      = "*"

[tasks]
smoke = "python scripts/smoke.py"
info  = "python scripts/info.py"
infer = "python scripts/infer.py"
test  = "pytest -q tests/"
lint  = "python -m compileall src scripts tests"
